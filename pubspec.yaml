name: rts_moba_flame
description: "RTS móvil (MOBA más adelante) — Flutter + Flame. v0.2.2: Energía (paneles solares) + reanudar construcción."
publish_to: 'none'
version: 0.2.2+4

environment:
  sdk: ^3.13.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flame: ^1.38.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/License.txt
    - assets/MANIFEST.md
